
>> Ax = [1 0 0; 0 cos(-20 * pi / 180) sin(-20 * pi / 180); 0 -sin(-20 * pi / 180) cos(-20 * pi / 180)];
>> raw=importdata('sensor-x-20.dat');
raw = raw';
raw = Ax*raw;
raw = raw';
x_m = raw(:,1);
y_m = raw(:,2);
z_m = raw(:,3);
>> figure
scatter3(x_m, y_m, z_m, 'fill', 'MarkerFaceColor', 'blue'); hold on;
xlabel('X-axis'); ylabel('Y-axis'); zlabel('Z-axis');
plot_sphere([0,0,0]', 40);
